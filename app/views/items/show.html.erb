<div class="container">
 
    <h1><%= @item.title %></h1><br>
    <%= @item.description %><br>

  <%= link_to "Back", root_path %> | <%= link_to "Edit", edit_item_path(@item) %> | <%= link_to "Delete", item_path(@item), method: :delete, data: {confirm: "Are you sure?"} %>
</div>